const lang = {
  english: 'en',
  french: 'fr',
  all: 'all',
  default: 'en'
};

const expoPNTokenPrefix = 'ExponentPushToken[';
const expoPNTokenSuffix = ']';

module.exports = {
  lang,
  expoPNTokenPrefix,
  expoPNTokenSuffix
};
