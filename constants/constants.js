const lang = {
  english: 'en',
  french: 'fr',
  default: 'en'
};

const expoPNTokenPrefix = 'ExponentPushToken[';
const expoPNTokenSuffix = ']';

module.exports = {
  lang,
  expoPNTokenPrefix,
  expoPNTokenSuffix
};
